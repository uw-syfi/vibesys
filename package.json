{
  "name": "vibesys-workspace",
  "private": true,
  "packageManager": "pnpm@11.11.0",
  "scripts": {
    "format:ts": "biome format --write .",
    "check:format:ts": "biome format .",
    "lint:ts": "biome lint .",
    "check:ts": "biome check .",
    "check:ts-architecture": "depcruise --config .dependency-cruiser.cjs clients/backend-client/src clients/core-state/src clients/tui/src && node scripts/check_ts_package_manifests.mjs",
    "test:ts-architecture": "node --test scripts/check_ts_architecture.test.mjs",
    "build:clients": "pnpm --filter @vibesys/tui... build",
    "check:clients": "pnpm --filter @vibesys/backend-client check && pnpm --filter @vibesys/core-state check && pnpm --filter @vibesys/tui check",
    "proto:lint": "buf lint",
    "proto:breaking": "node scripts/check_proto_breaking.mjs",
    "proto:generate": "buf generate",
    "proto:check": "buf generate && git diff --exit-code -- src/server/wire/v2 clients/backend-client/src/gen",
    "test:clients": "pnpm test:ts-architecture && pnpm --filter @vibesys/backend-client test && pnpm --filter @vibesys/core-state test && pnpm --filter @vibesys/tui test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@bufbuild/buf": "^1.73.0",
    "@bufbuild/protoc-gen-es": "^2.15.0",
    "dependency-cruiser": "16.10.4"
  }
}
