interface:
  display_name: "VibeServe Open PR"
  short_description: "Open VibeServe pull requests"
  default_prompt: "Use $vs-open-pr to prepare, push, and open a VibeServe pull request for the current changes."
