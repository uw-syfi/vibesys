# vs-project

Owns the filesystem contract for a repository-native VibeSys project. It
discovers and validates task definitions and provides access to generated
project and run state under `.vibesys/`.
