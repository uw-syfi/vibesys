# vs-sandbox

Provides execution environments for agent workspaces, including local, confined
host, and Docker backends. It also owns host resource declarations, workspace
path policy, sandbox lifecycle hooks, and Modal model-weight volume provisioning.
