# vs-evaluator-protocol

Reads and validates evaluator result streams for VibeSys. It owns the typed
protocol records, measurement validation, and checks that an evaluator declares
the metrics a task uses as objectives.
