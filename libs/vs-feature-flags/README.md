# vs-feature-flags

Provides typed feature flag definitions, evaluation, and configuration parsing.
It validates flag manifests and overrides so applications can use enum members
consistently at call sites.
