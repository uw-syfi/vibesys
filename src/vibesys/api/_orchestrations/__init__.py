"""Transitional adapters for built-in orchestrations.

Implementation behavior remains owned by ``vibesys.loops``. These adapters
translate the current public ``RunRequest`` until the common request and
runtime boundaries are migrated.
"""
