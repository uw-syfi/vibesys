## Task objective

{{ objective }}

Implement and validate the candidate against this objective and the task's
candidate contract. Treat the objective and repository documentation as the
source of truth for required behavior.

## Acceptance criteria

{% if reference_path %}
- Use the reference material at `{{ reference_path }}` where it helps explain the task contract.
{% endif %}
{% if accuracy_command %}
- The configured correctness checker passes: `{{ accuracy_command }}`.
{% endif %}
{% if benchmark_command %}
- The configured benchmark completes and reports its declared metric: `{{ benchmark_command }}`.
{% endif %}
- Add or update focused tests for behavior you change, and run the relevant tests.
{% if runtime_notes %}

## Runtime notes

{{ runtime_notes }}
{% endif %}
