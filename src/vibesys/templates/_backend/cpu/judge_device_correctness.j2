Does the code run on the CPU (`device="cpu"`) with no GPU (`cuda`/`mps`) dependency? This is a CPU-only target, so any GPU device placement or GPU-only library requirement is a correctness failure, not an optimization.
