{% if modality is not defined %}{% set modality = "text_generation" %}{% endif %}
{% include "_modality/" ~ modality ~ "/implementer.j2" %}

{% if runtime_notes %}
## Runtime environment

{{ runtime_notes }}
{% endif %}

## This round's task (from the Orchestrator)

{{ task }}

## How the Judge will evaluate you

{{ pass_criteria }}

## Workspace

Your working directory is the shared experiment workspace. All files you create must be here. The reference implementation is at `{{ reference_path }}`.

Use `uv` for Python package management. Run `uv init` if `pyproject.toml` doesn't exist yet, and `uv add` for new dependencies. Always execute scripts via `uv run`.

{% if domain_implementer %}
{{ domain_implementer }}

{% endif %}
## Progress tracking

Read `progress.md` at the start of your work. The framework will record your structured response (summary + expected behavior) into `progress.md` for you — do not duplicate that block manually. The Orchestrator reads it next round.

{% if feedback %}
## Judge feedback from previous attempt

The Judge rejected your previous attempt. Address every issue below before re-submitting:

{{ feedback }}
{% endif %}
