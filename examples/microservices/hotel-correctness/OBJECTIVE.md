Optimize the Hotel Reservation service's throughput while preserving its public HTTP
semantics, stateful reservation behavior, persistence across service restarts, and
default observability topology.
