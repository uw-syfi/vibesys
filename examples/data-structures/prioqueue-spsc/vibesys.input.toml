version = 1

[agent]
domain = "generic"

[workspace]
seed = "../../starters/priority-queue-rs"

[evaluator]
source = "../../evaluators/priority-queue"

[accuracy]
command = [
  "go", "-C", "_evaluator/priority-queue",
  "run", ".", "check", "--workspace", "../..",
  "--scenario", "spsc", "--operations", "32", "--trials", "100",
]
timeout_seconds = 120

[benchmark]
command = [
  "go", "-C", "_evaluator/priority-queue",
  "run", ".", "benchmark", "--workspace", "../..",
  "--scenario", "spsc", "--repetitions", "3",
]
timeout_seconds = 300

[benchmark.result]
json_argument = "--output-json"
metric = "total_ops_per_sec"
