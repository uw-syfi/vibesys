version = 1

[agent]
domain = "generic"

[evaluator]
source = "../../evaluators/ordered-map"

[accuracy]
command = [
  "go", "-C", "_evaluator/ordered-map",
  "run", ".", "check", "--workspace", "../..",
  "--scenario", "mw", "--operations", "24", "--trials", "100",
]
timeout_seconds = 120

[benchmark]
command = [
  "go", "-C", "_evaluator/ordered-map",
  "run", ".", "benchmark", "--workspace", "../..",
  "--scenario", "mw", "--repetitions", "3",
]
timeout_seconds = 300

[benchmark.result]
json_argument = "--output-json"
metric = "total_ops_per_sec"
