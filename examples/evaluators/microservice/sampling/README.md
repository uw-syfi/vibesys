# Shared sampling

This package contains deterministic sampling primitives that must behave the
same way in benchmark and accuracy modes. Application-specific fixture grammar
and semantic case construction remain in application adapters or mode-neutral
application support packages.
